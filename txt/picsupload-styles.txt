/* picsupload-styles.css */

/* General Body and Font Styles */
html, body {
    margin: 0;
    padding: 0;
    height: 100%;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
                 Helvetica, Arial, sans-serif, "Apple Color Emoji",
                 "Segoe UI Emoji", "Segoe UI Symbol";
    background-color: #f4f4f9;
}

/* Header and Controls Section */
.header-controls {
    background-color: #fff;
    padding: 15px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    margin-bottom: 15px;
}

.header-controls h1 {
    margin: 0 0 10px 0;
    color: #333;
    font-size: 1.5em;
}

#status {
    padding: 8px;
    border-radius: 5px;
    margin-bottom: 10px;
    text-align: center;
    font-weight: bold;
    color: white;
    font-size: 0.9em;
}

/* Grid Layout for Controls */
.controls-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 15px;
    align-items: end;
}

.controls-grid > div {
    display: flex;
    flex-direction: column;
}

.controls-grid label {
    display: block;
    margin-bottom: 5px;
    font-weight: bold;
    color: #555;
    font-size: 0.9em;
}

.controls-grid input {
    width: 100%;
    padding: 8px;
    box-sizing: border-box;
    border: 1px solid #ccc;
    border-radius: 5px;
    font-size: 0.9em;
}

/* --- DATEI-UPLOAD STYLING --- */

/* Versteckt den Standard "Durchsuchen..."-Button */
.controls-grid input[type="file"] {
    display: none;
}

/* Gestaltet das Label wie einen Button */
.controls-grid label[for="imageUpload"] {
    background-color: #007bff;
    color: white;
    padding: 10px;
    text-align: center;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.2s;
    font-weight: normal;
    margin-top: auto;
}

.controls-grid label[for="imageUpload"]:hover {
    background-color: #0056b3;
}

/* --- ENDE DATEI-UPLOAD STYLING --- */

/* Button Container Styling */
.button-container {
    grid-column: 1 / -1; /* Span all columns */
    display: flex;
    justify-content: space-between;
    margin-top: 10px;
}

/* Upload Button Styling */
#uploadButton {
    padding: 10px 20px;
    background-color: #28a745;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1em;
    font-weight: bold;
    transition: background-color 0.2s;
}

#uploadButton:hover {
    background-color: #218838;
}

/* Back Button Styling */
#backButton {
    padding: 10px 20px;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.2s;
}

#backButton:hover {
    background-color: #0056b3;
}

/* Preview Container Styling */
#previewContainer {
    margin-top: 10px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

#previewContainer img {
    max-width: 100px;
    max-height: 100px;
    border: 1px solid #ccc;
    border-radius: 5px;
    object-fit: cover;
}